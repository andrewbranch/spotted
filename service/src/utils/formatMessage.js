/* @flow */

import type { Model } from '../types/mongoose';
import type { Rule } from '../models/rule';
import type { SpotData } from '../types/spotData';

export default (rule: Model<Rule>, data: SpotData) => {
  
};
