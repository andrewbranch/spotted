/* @flow */

import type { SpotData } from '../types/spotData';
import type { PopulatedRule } from '../models/rule';
import Rule from '../models/rule';

export default (data: SpotData): PopulatedRule => {
  
};
